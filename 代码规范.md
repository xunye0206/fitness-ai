# 代码规范（讨论稿）

> 本规范用于约定「健身 AI Agent」项目的代码风格与协作底线。在动手写实现代码前先对齐，避免返工。
> 状态：草稿，待讨论定稿。任何条目都可改。

---

## 1. 原则

- **先设计后代码**：功能、数据模型、接口边界先在讨论中定稿，再落地。
- **配置驱动，不写死**：模型、供应商、阈值等通过配置注入，业务代码不硬编码。
- **小步可跑**：优先跑通最小链路，再扩展；不为未来可能性提前堆抽象。
- **可插拔优先于绑定**：外部依赖（LLM、存储、推送）一律走接口，便于替换与测试。
- **失败要降级，不崩溃**：任何外部调用（LLM / 可穿戴 / 推送）失败都要有兜底。

---

## 2. 技术栈基线

- 后端：Python 3.13（托管运行时）、FastAPI、LangGraph、Pydantic / pydantic-settings
- 存储：SQLite 单文件（结构化：饮食 / 运动 / 睡眠 / 用户）+ Markdown wiki 页（叙事记忆：画像 / 反思 / 洞察，由 Agent 维护）；向量检索按需挂轻量本地索引（个人体量初期可暂缓）；取消 Redis 依赖
- 调度：APScheduler（推送事件巡检）
- 部署：Docker Compose 一键启动，Gunicorn + Uvicorn workers
- 前端：微信小程序（原生 / TypeScript），一期只做记录与触达

---

## 3. 目录结构与分层约定（后端）

采用「功能模块（垂直切片）+ 清晰内部分层 + Agent 横切编排」。每个功能模块内部按 api → service → domain 分层；Agent 为最上层编排者，模块通过 skill 注册表向它暴露能力；所有外部 IO 收口到 `integrations/`。

```
app/
  config.py            # 配置：按用途拆分供应商，读 .env
  core/                # 横切基础设施
    db.py              # SQLite 连接 / 会话
    security.py        # 鉴权 / 当前用户
    logging.py         # 结构化日志
  llm/                 # LLM 接口层（见 §6）
    base.py            # LLMProvider 抽象 + Capability 枚举
    providers/         # 各供应商适配（OpenAI 兼容 / 其他）
    registry.py        # 按能力装配
    router.py          # 业务入口：reason / see / embed
  integrations/        # 外部 IO 收口（不进业务层）
    health.py          # 厂商健康云桥接（二期）
    weather.py         # 天气
    wechat.py          # 微信推送
  agent/               # Agent 横切编排
    graph.py           # LangGraph 状态机
    guardrails.py      # 量化红线拦截
    skills.py          # skill 注册表（模块在此注册能力）
  modules/             # 功能模块（垂直切片，每模块内 api/service/domain）
    auth/              # 登录 / 用户（横切级，被各模块依赖）
      api.py  service.py  domain.py
    diet/              # 识图饮食
      api.py  service.py  domain.py  skill.py   # skill 暴露给 Agent
    training/          # 训练录入
      api.py  service.py  domain.py  skill.py
    report/            # 日报（Agent 产物的消费者 + 存储）
      api.py  service.py  domain.py
    push/              # 事件投递（含 APScheduler 巡检）
      api.py  service.py  domain.py  scheduler.py
  wiki/                # Markdown 叙事记忆页（Agent 读写，不存数值）
tests/                 # 与模块对齐的测试
```

**分层红线**
- 模块间不直接 import 业务代码；跨模块协作一律通过 `agent/skills.py` 注册表与 `core`。
- 外部依赖不直接出现在 module 的 service 里，统一走 `integrations/`。
- 数值 / 日志永远存 SQLite，不准进 wiki；wiki 只放话术级记忆（画像、反思、洞察结论）。

---

## 4. 命名与类型

- Python：函数/变量 `snake_case`，类 `PascalCase`，常量 `UPPER_SNAKE`。
- **类型注解必写**：函数签名含参数与返回值类型。
- 小程序：目录与组件 `kebab-case`，变量 `camelCase`。
- 避免缩写歧义（如 `u`/`tmp` 仅限极短作用域）。

---

## 5. 异步与 IO

- 路由与 IO 密集逻辑统一 `async def`；使用异步客户端（DB / Redis / HTTP）。
- 阻塞调用（同步 SDK）放入线程池，禁止在事件循环里阻塞。
- LLM 调用必须设超时与重试上限，失败进入降级分支。

---

## 6. LLM 使用红线（重要）

- 业务代码**只能**通过 `app.llm.router` 的 `reason / see / embed` 调用，禁止直接 `import openai` 或某个 provider。
- 供应商与模型由 `.env` 决定（推理 / 视觉 / 嵌入可各接不同 API），换模型不改业务代码。
- 非 OpenAI 兼容的供应商（如 Gemini）需新建子类继承 `LLMProvider`，注册进 `registry`。
- 每次 LLM 调用记录：用途、模型、耗时、是否降级。
- **不把系统提示词或用户敏感数据原文透传给不可信第三方**；建议输出须附依据与免责声明。

---

## 7. 配置与密钥

- 密钥与敏感配置放 `.env`，通过 `pydantic-settings` 读取；`.env` 不入库。
- 提交 `.env.example`（仅占位，无真实密钥）。
- 禁止在代码、日志、提交信息中出现真实 API key。

---

## 8. 错误处理与日志

- 结构化日志（级别 + 上下文），不打印密钥与完整 PII。
- 不吞异常；对外错误返回统一结构，不泄露内部栈。
- 外部依赖失败 → 降级（缓存值 / 提示稍后重试 / 跳过该次推送），不中断主流程。

---

## 9. 存储约定

- **结构化数据 → SQLite 单文件**（用户 / 饮食 / 训练 / 睡眠 / 推送记录）。零运维、个人体量完全够用，干掉 Postgres 服务与 pgvector 部署负担。
- **叙事记忆 → Markdown wiki 页**（`wiki/` 目录），由 Agent 读写维护：用户画像、个性化反思（"一熬夜第二天必练崩"）、周报结论、累计洞察。
- **向量检索按需**：个人体量初期可暂缓；后续如需语义检索（历史日志 / 知识库 RAG / 食物相似召回）再挂轻量向量索引（如 sqlite-vec / 本地嵌入）。
- **红线**：数值 / 日志只进 SQLite，绝不进 wiki；wiki 仅承载话术级记忆，避免 LLM 把关键数字算飞。
- 迁移用 Alembic（或简单版本脚本）管理，禁止手写散落 SQL。

---

## 10. 测试约定

- `tests/` 与 `app/` 模块对齐；单元测试不依赖网络与外部密钥。
- 冒烟测试可纯内存运行（用 fake provider / 本地配置）。
- 提交前至少跑：`py_compile` 全量 + `pytest`。
- LLM / 推送等外部交互以 mock 或 fake 适配器覆盖，不触发真实请求。

---

## 11. 提交与分支

- `main` 受保护；功能走 `feature/xxx` 分支，完成后合并。
- 提交信息说明**意图与原因**，不只写"fix"/"update"。
- 一次提交聚焦一件事；大型改动拆小。

---

## 12. 合规与隐私底线

- 健康 / 饮食 / 照片属敏感个人信息，存储加密、最小采集、明确授权。
- 输出含「非医疗诊断」免责。
- 内测可用小程序「工具」类目，正式收费前补齐相应资质。

---

_待讨论项（可选）_：ORM 选型（SQLAlchemy vs asyncpg 原生）、是否引入 Ruff/Black 统一格式、小程序状态管理方案、LangGraph 状态字段落库方式。

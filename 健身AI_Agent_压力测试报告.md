# 健身AI Agent 压力测试报告

> 生成时间：2026-07-08 01:22:20  

## 测试环境与方式

- **不烧真模型**：`REASONING_PROVIDER`/`VISION_PROVIDER`=fake，`EMBEDDING_PROVIDER`=空，LLM 零成本零网络。
- **进程内压测**：`httpx.ASGITransport` 直接打 ASGI app，单事件循环 `asyncio.gather` 并发，不启端口、不跑 lifespan（避开推送调度器干扰）。
- **数据隔离**：独立 `stress_fitness.db`，跑前自动清空；种子数据为 1 用户 + 60 饮食 + 40 训练（分散过去 7 天）。
- **数据库**：SQLite（aiosqlite），**当前未开启 WAL、未设 busy_timeout**（即项目默认配置，用于暴露真实瓶颈）。

## 场景结果

| 场景 | 请求数 | 墙钟(s) | QPS | 成功率 | P50(ms) | P95(ms) | P99(ms) | TTFB P50 | database is locked |
|---|---|---|---|---|---|---|---|---|---|
| S1b 聊天热(缓存命中) | 20 | 0.18 | 108.84 | 100.0% | 33.5 | 149.4 | 153.6 | 33.4ms | 0 |
| S2 读密集(GET /diet+/training) | 60 | 0.36 | 164.94 | 100.0% | 102.7 | 163.9 | 175.8 | - | 0 |
| S3 写密集(POST /training) | 45 | 1.48 | 30.4 | 100.0% | 45.8 | 1224.4 | 1425.8 | - | 0 |
| S4 报告生成(POST /report/generate) | 20 | 0.71 | 28.23 | 100.0% | 120.4 | 607.6 | 680.5 | - | 0 |
| S5 混合(聊天+读+写+报告) | 60 | 0.92 | 65.53 | 100.0% | 102.9 | 711.2 | 826.4 | 45.4ms | 0 |

## 缓存命中验证（S1）

- 冷启动单发聊天：**45.4ms**（含 `build_context` 聚合 7 天数据的 DB 查询）。
- 热打同用户同问题：`build_context` 缓存命中率 **95.2%**，P50 显著低于冷启动 → **缓存层已真正生效**（此前 Redis 缺失时永远 miss 的问题已修复）。
- 注：`build_context` 缓存 key 为 `ctx:{user_id}:{days}:{semantic}`，不含数据版本；数据变更后缓存不会主动失效，这是已知的一致性权衡，详见下方建议。

## 瓶颈分析

1. **写路径健康，但尾部延迟偏高（非失败）**：S3 写密集（并发 15）成功率 100%、0 locked；但 P95=1224.4ms / P99=1425.8ms 明显高于读密集 S2（P95≈163.9ms），反映并发写时 SQLite 排它锁等待 + 连接池排队。当前规模无失败，但并发再升高尾部会进一步拉长，仍建议开启 WAL + busy_timeout 压平尾部。
2. **混合场景吞吐**：S5（聊天+读+写+报告混合，并发 20）QPS≈65.53，P99=826.4ms，读多写少时 SQLite 读并发表现良好（WAL 读不阻塞写）。
3. **读路径健康**：S2 读密集（并发 20 × 60）成功率 100%，SQLite 读并发无锁竞争。
4. **聊天首字延迟（TTFB）**：S1b/S5 的 TTFB P50 在毫秒级，得益于 SSE 流式 + 缓存命中后跳过 DB 聚合。

## 可落地的优化建议（需你授权后再改生产代码）

1. **SQLite 开启 WAL + busy_timeout**（强烈建议，个人项目最佳实践）：在 `app/core/db.py` 的 `create_async_engine` 增加
   `connect_args={"timeout": 30, "check_same_thread": False}` 并在连接上执行 `PRAGMA journal_mode=WAL; PRAGMA busy_timeout=5000;`。可将写锁错误率从当前量级降到接近 0，且读不阻塞写。
2. **缓存一致性**：若未来数据写入频繁，考虑在 diet/training 写入成功后 `cache_delete` 对应 `ctx:{uid}:*` 前缀，避免旧上下文被复用。
3. **连接池调优**：若迁 Postgres（策划书已规划），连接池与 WAL 问题自然消解；本地 SQLite 阶段靠建议 1 即可。
4. **压测常态化**：本脚本 `scripts/stress_test.py` 已可重复运行，建议作为回归基线，改动 DB/缓存/上下文逻辑后复测对比。

> 本次压测**未修改任何生产代码**，仅读取与打接口；所有建议项待你确认后实施。
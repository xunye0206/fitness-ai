# 健身 AI Agent · 项目策划书（升级版）

> 定位说明：本项目为**个人级工程项目**，目标是做出一套可运行、体验闭环的 AI 健身教练，不追求大规模商业化体量。本版在初稿基础上补齐了缺失板块、修正了技术判断、并收紧了范围（商业化仅确认"用户为何付费"）。

---

## 一、项目定位

7×24 小时在线的 AI 健身教练 —— 不只是一个记录工具，而是在用户想放弃时推一把、练过头时拉一把的智能伙伴。

与"被动记录型 App"的本质区别：**主动干预 + 用户确认**。用户付出的是 ¥9.9–29.9/月，买到的是"一个一直在、会推、会拉"的陪伴式教练（对标真人私教 ¥200–500/节的一个零头）。

---

## 二、用户为何付费（极简商业化）

个人项目，无需完整商业计划。付费逻辑锚定一个痛点：**真人私教太贵、免费 App 记三天就弃**。付费理由按说服力排序：

| 排序 | 付费理由 | 用户感知 |
|---|---|---|
| 1 | 陪伴 / 问责感 | "有人一直盯着我"，免费记录 App 给不了 |
| 2 | 零摩擦记录 | 拍照 10 秒记饮食，干掉头号流失原因（手动录入太累） |
| 3 | 多源交叉洞察 | "你睡差+吃不够→今天练崩了"，普通 App 各记各的不交叉 |
| 4 | 私教平替锚定 | 价格=私教零头，心理账户里是"超便宜的教练" |

**计费落地（极简）**：小程序原生微信支付 / 后期 Stripe，无需 billing 中台。建议内测期免费验证留存，后再开通订阅。

---

## 三、核心功能（三位一体 · 细化）

| 模块 | 功能 | 用户价值 | 关键设计 |
|---|---|---|---|
| 🍽️ 智能饮食 | 拍照识别食物热量（AI 估算 + 用户修正） | 10 秒完成饮食记录 | GPT-4o 视觉估算热量/宏量 + **置信度**；用户一键修正；**修正数据回流校准**（飞轮） |
| ⌚ 运动追踪 | 自动同步消耗 + 手动录入训练 | 被动采集 + 主动补充，日志完整 | 一期：微信运动步数 + 手动录入；二期：原生 App / 厂商云桥接自动同步 |
| 🤖 Agent 日志 | 每日自动报告 + 趋势洞察 + 建议 | 从"记录"升级为"复盘指导" | 摄入/消耗/缺口/睡眠/趋势 + 周报 + 个性化建议 |
| 🔔 主动推送 | 事件驱动型提醒（见第五节事件表） | 关键时机精准干预，非定时骚扰 | 每日 ≤3 条；基于多源状态变化触发 |

---

## 四、技术架构（轻量 · 务实 · 已修正）

```
用户层：微信小程序（轻量记录 / 推送触达）
        + 原生 App / 厂商云桥接（可穿戴自动同步，二期）
            ↓
接入层：FastAPI（Gunicorn + Uvicorn workers）
            ↓
大脑层：LangGraph（状态机驱动推理）+ 安全护栏 Guardrails
            ↓
工具层：GPT-4o 视觉识别  ·  推送引擎（APScheduler 事件巡检）  ·  厂商健康云桥接
            ↓
存储层：SQLite 单文件（结构化：饮食/运动/睡眠/用户）
        Markdown wiki 页（叙事记忆：画像/反思/洞察，Agent 维护）
        （向量检索按需挂轻量索引，个人体量初期可暂缓）
```

**核心设计原则（修正后）**
- 用户量小 → 单库搞定，不引入 Milvus 等重型组件。
- 生产部署 → Docker Compose 一键启动；**Gunicorn 多进程 + Uvicorn workers**（`uvicorn.workers.UvicornWorker`）。
- 主动推送 → APScheduler 定时巡检 + 事件触发，每日 ≤3 条。
- 代码组织 → 功能模块垂直切片（auth/diet/training/report/push，每模块内 api→service→domain）+ `agent/` 横切编排（LangGraph + 护栏，模块经 skill 注册表暴露能力）+ `integrations/` 收口外部 IO；详见《代码规范》。

**两处原稿模糊点的明确（已随体量收敛）**
- **向量检索用途（按需）**：①用户历史日志语义检索（"上次膝盖疼那次怎么调的"）；②健身知识库 RAG；③食物图像 embedding 相似召回。个人体量初期可暂缓，需要时再挂轻量本地索引。
- **存储分工**：结构化数值（饮食/运动/睡眠/用户）存 SQLite；叙事记忆（画像/反思/洞察）存 Markdown wiki 页由 Agent 维护。两者职责清晰，不混用、不让 LLM 去加总关键数值。

**LangGraph 状态机主线**：采集 → 评估（护栏检查）→ 推理 → 生成 → 推送决策 →（用户反馈回流）。

> ⚠️ 关键架构修正：微信小程序**无法原生读取** Health Connect / HealthKit。可穿戴自动同步必须走「原生 App」或「厂商开放平台云桥接」（华为 / 小米运动健康云、Apple 需桥接服务）。因此小程序一期先用「微信运动步数 + 手动录入」顶上，自动同步放到二期原生 / 云桥接路线。

---

## 五、主动推送事件表（具象化）

不再写"基于多源数据的事件驱动"这种空话，直接列出事件：

| 事件 | 触发条件 | 动作 |
|---|---|---|
| 恢复预警 | 静息心率连续 3 天 ↑ | 建议降强度 / 休息 |
| 睡眠不足 | 昨睡 <5h 且今日有训练计划 | 建议调整计划 |
| 饮食缺口 | 连续 2 天热量缺口 >500 kcal | 提醒加餐 |
| 天气替代 | 原计划户外 + 暴雨 / 高温预警 | 给室内替代方案 |
| 放弃预警 | 连续 2 天无记录 | 轻量鼓励，不施压 |
| 正向反馈 | 周目标达成 | 正向强化 |

---

## 六、Agent 安全护栏（Guardrails · 必须独立一层）

"练过头时拉一把"不能靠 LLM 自由发挥，需**量化红线**：

- **触发恢复预警**：静息心率连续 3 天 ↑ / HRV 持续 ↓ / 睡眠负债累计 / 连续训练 ≥6 天。
- **拦截危险建议**：检测到伤病信号（关节疼痛记录、异常心率）→ 禁止"加量"类建议，改建议休息 / 就医提示。
- **建议带依据与免责**：每条建议附数据依据，并标注"非医疗诊断"。

---

## 七、合规与隐私（敏感个人信息）

- 健康 / 饮食 / 食物照片属敏感个人信息，受《个人信息保护法》约束。
- 数据最小化、传输与存储加密、明确授权链路。
- 免责声明：本产品**不提供医疗诊断**。
- 小程序"医疗健康"类目需资质：内测期可用"工具"类目规避，正式收费前补齐。
- 用户数据可导出、可删除。

---

## 八、成功指标（北极星）

- **北极星**：饮食记录 7 日留存 / 主动建议采纳率。
- 辅助：次日推送点击率、周报打开率、干预后训练完成率。
- **冷启动处理**：初始问卷建基线画像 + 先用通用知识给建议 + 明示"数据越多越准"，避免前两周无数据时空转。

---

## 九、开发计划（MVP 三阶段 · 修正后）

| 阶段 | 周期 | 交付物 |
|---|---|---|
| 一期 | 2 周 | **垂直切片优先**：拍照→估算→修正→存库→次日报告单链路 + 手动训练录入 + 基础推送（每日 2 次固定 + 1 事件） |
| 二期 | 1 周 | 接入厂商云 / 原生自动同步 + 护栏恢复预警 + 事件驱动推送完善 |
| 三期 | 1 周 | 周报生成 + 用户画像动态调整 + Docker 部署上线 |

> 周期提示：2 周做完识食 + 录入 + 推送（含 LangGraph + 小程序 + GPT-4o + 推送引擎）对单人偏紧。故一期强调**先跑通单链路再扩展**，降低返工风险。

---

## 十、风险与降级

- 小程序读不到可穿戴 → 一期用微信运动 + 手动录入兜底。
- 识食误差大 → 置信度 + 修正飞轮 + 建议免责。
- API 限流 / 断连 → 本地缓存 + 重试 + 手动录入兜底。
- 用户忽略推送 → 降频，不为点击率牺牲体验。

---

## 十一、一句话总结

用 Agent 的"主动思考"替代 App 的"被动记录"，让每一次推送都像私教在耳边提醒 —— 做一个个人能写完、跑得起来的陪伴式健身教练。
